# This package is NOT part of the pnpm workspace.
# It must be installed and built independently on Windows (or macOS/Linux for cross-compile).
#
# Usage:
#   cd artifacts/academia-os-desktop
#   pnpm install
#   pnpm dist:win

# Allow native modules to be rebuilt for the target platform
node-linker=hoisted
