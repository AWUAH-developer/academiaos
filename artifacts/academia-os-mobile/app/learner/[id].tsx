import React, { useCallback, useMemo } from 'react';
import { Pressable, RefreshControl, StyleSheet, Text, View } from 'react-native';
import { router, useLocalSearchParams } from 'expo-router';
import { AppScreen } from '@/components/AppScreen';
import { Avatar, Badge, Card, EmptyState, ErrorState, LoadingState, Metric, ScreenTitle } from '@/components/ui';
import { getAttendance, getFees, getLearners, getPayments, getReports, getResults } from '@/api/client';
import { useLoad } from '@/hooks/useLoad';
import { learnerName, money, shortDate } from '@/lib/format';
import { canRecordAttendance } from '@/lib/permissions';
import { useAuth } from '@/auth/AuthContext';
import { colors, spacing } from '@/theme';
export default function LearnerDetails(){const{id}=useLocalSearchParams<{id:string}>();const{user}=useAuth();const loader=useCallback(async()=>{const[learners,attendance,fees,payments,results,reports]=await Promise.all([getLearners(id),getAttendance(id,120),getFees(id).catch(()=>({summary:[],charges:[]})),getPayments(id).catch(()=>[]),getResults(id).catch(()=>[]),getReports(id).catch(()=>[])]);return{learner:learners[0],attendance,fees,payments,results,reports};},[id]);const{data,loading,error,refreshing,reload}=useLoad(loader,[id]);const currency=user?.school?.currency||'GHS';const balance=Number(data?.fees.summary[0]?.balance||0);const attendanceSummary=useMemo(()=>({present:data?.attendance.filter(a=>a.status==='PRESENT').length||0,absent:data?.attendance.filter(a=>a.status==='ABSENT').length||0,late:data?.attendance.filter(a=>a.status==='LATE').length||0}),[data]);if(loading&&!data)return <AppScreen scroll={false}><LoadingState/></AppScreen>;if(error&&!data)return <AppScreen><ErrorState message={error} onRetry={reload}/></AppScreen>;if(!data?.learner)return <AppScreen><EmptyState title="Learner unavailable" message="This record is not linked to your account."/></AppScreen>;const l=data.learner;return <AppScreen refreshControl={<RefreshControl refreshing={refreshing} onRefresh={reload}/>}><View style={styles.hero}><Avatar uri={l.photoUrl} name={learnerName(l.firstName,l.lastName)} size={88}/><View style={styles.grow}><Text style={styles.name}>{learnerName(l.firstName,l.lastName)}</Text><Text style={styles.meta}>{l.className||'Class not assigned'}{l.classStream?` · ${l.classStream}`:''}</Text><Text style={styles.admission}>{l.admissionNo}</Text><Badge text={l.status.toLowerCase()} tone={l.status==='ACTIVE'?'success':'warning'}/></View></View>{canRecordAttendance(user?.role)?<Pressable onPress={()=>router.push({pathname:'/attendance-record',params:{learnerId:l.id,learnerName:learnerName(l.firstName,l.lastName)}})}><Card style={styles.attendanceAction}><Text style={styles.actionTitle}>Record today’s attendance</Text><Text style={styles.actionCopy}>Available because your role has attendance permission.</Text></Card></Pressable>:null}<View style={styles.metrics}><Metric label="Present" value={attendanceSummary.present}/><Metric label="Absent" value={attendanceSummary.absent}/><Metric label="Late" value={attendanceSummary.late}/><Metric label="Fee balance" value={money(balance,currency)}/></View><Section title="Recent attendance">{!data.attendance.length?<Text style={styles.muted}>No attendance records.</Text>:data.attendance.slice(0,8).map(item=><View key={item.id} style={styles.line}><Text style={styles.lineMain}>{shortDate(item.date)}</Text><Badge text={item.status.toLowerCase().replaceAll('_',' ')} tone={item.status==='PRESENT'?'success':item.status==='ABSENT'?'danger':'warning'}/></View>)}</Section><Section title="Fee charges">{!data.fees.charges.length?<Text style={styles.muted}>No fee charges available.</Text>:data.fees.charges.slice(0,8).map(item=><View key={item.id} style={styles.line}><View style={styles.grow}><Text style={styles.lineMain}>{item.description}</Text><Text style={styles.muted}>{item.category||'School charge'} · {shortDate(item.createdAt)}</Text></View><Text style={styles.amount}>{money(item.amount,currency)}</Text></View>)}</Section><Section title="Payment receipts">{!data.payments.length?<Text style={styles.muted}>No payments available.</Text>:data.payments.slice(0,8).map(item=><View key={item.id} style={styles.line}><View style={styles.grow}><Text style={styles.lineMain}>{item.receiptNo||item.reference||'Payment'}</Text><Text style={styles.muted}>{item.method} · {shortDate(item.createdAt)}</Text></View><Text style={styles.positive}>{money(item.amount,currency)}</Text></View>)}</Section><Section title="Approved results">{!data.results.length?<Text style={styles.muted}>No approved results available.</Text>:data.results.slice(0,12).map(item=><View key={item.id} style={styles.line}><View style={styles.grow}><Text style={styles.lineMain}>{item.subject}</Text><Text style={styles.muted}>{item.term} · {item.academicYear}</Text></View><Text style={styles.score}>{item.totalScore??'–'}{item.grade?` · ${item.grade}`:''}</Text></View>)}</Section><Section title="Published terminal reports">{!data.reports.length?<Text style={styles.muted}>No published reports available.</Text>:data.reports.map(item=><View key={item.id} style={styles.line}><View style={styles.grow}><Text style={styles.lineMain}>{item.term} · {item.academicYear}</Text><Text style={styles.muted}>Published {shortDate(item.publishedAt)}</Text></View><Badge text={item.status.toLowerCase()} tone="success"/></View>)}</Section></AppScreen>}
function Section({title,children}:{title:string;children:React.ReactNode}){return <><ScreenTitle title={title}/><Card>{children}</Card></>}
const styles=StyleSheet.create({hero:{flexDirection:'row',gap:spacing.lg,alignItems:'center',backgroundColor:colors.navy,padding:spacing.lg,borderRadius:20},grow:{flex:1},name:{fontSize:22,fontWeight:'900',color:'#fff'},meta:{color:'#D8E4F3',marginTop:4},admission:{color:'#8FE3C0',fontWeight:'800',marginVertical:5},attendanceAction:{backgroundColor:colors.greenSoft,borderColor:colors.green},actionTitle:{fontWeight:'900',color:colors.green},actionCopy:{color:colors.text},metrics:{flexDirection:'row',flexWrap:'wrap',gap:spacing.md},line:{flexDirection:'row',alignItems:'center',gap:spacing.md,paddingVertical:10,borderBottomWidth:1,borderBottomColor:colors.border},lineMain:{fontWeight:'800',color:colors.text},muted:{fontSize:12,color:colors.muted,marginTop:3},amount:{fontWeight:'900',color:colors.text},positive:{fontWeight:'900',color:colors.success},score:{fontSize:16,fontWeight:'900',color:colors.navy}});
